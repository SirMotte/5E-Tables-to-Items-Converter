# Environment variables for foundry-tables-to-items module development

# Set this to your GitHub project (owner/repo) for release URLs
# GH_PROJECT=username/foundry-tables-to-items

# GitHub tag for download URL (will be set by CI)
# GH_TAG=1.0.0

# Custom path to Foundry VTT Data/modules directory (optional)
# FOUNDTY_VTT_DATA_MODULES_PATH=C:\path\to\FoundryVTT\Data\modules

# Module version override (optional, defaults to package.json version)
# MODULE_VERSION=1.0.0